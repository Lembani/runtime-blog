<%= stylesheet_link_tag('userpartial') %>
<section class="user-partial">
  <%= image_tag('user.png', class:'user_picture') %>

  <div class="content">
    <h2><%= link_to(user.name, user_path(user)) %></h2>
    <p>
      Number of Posts: <%= user.posts_counter%>
    </p>
  </div>
</section>